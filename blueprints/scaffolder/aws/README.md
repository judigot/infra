# Scaffolder AWS blueprint

Dedicated deployment blueprint for `judigot/scaffolder` on `judigot.com`.

Initial target: one EC2 instance running the Scaffolder application behind Nginx, with Route 53 DNS managed separately by Terraform. The application repository remains read-only.
