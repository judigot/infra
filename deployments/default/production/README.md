# Default — production

Default AWS EC2 application deployment for the production environment.

Use `blueprints/app/aws` as the Terraform root architecture. Keep real deployment values in HCP Terraform workspace variables or an ignored local `terraform.tfvars`.
