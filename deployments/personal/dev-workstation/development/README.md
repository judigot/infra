# development

Personal development workstation deployment.

Uses `blueprints/app/aws` with development-specific values supplied locally or through the HCP Terraform workspace. Real `*.tfvars` files are intentionally not committed.
