# production

Personal production/demo application deployment.

Uses `blueprints/app-database/aws`. Select the database engine and production sizing through local variables or the HCP Terraform workspace. Real `*.tfvars` files are intentionally not committed.
